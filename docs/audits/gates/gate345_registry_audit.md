# Gate 345 Registry Audit — Vacuum Parameter Census / Minimal Input Theorem

## Gate identity

- **Gate:** 345
- **Package:** `pkg/bridge/vacuumparametercensus`
- **Theorem:** `VacuumParameterCensusMinimalInputTheoremAuditTheorem`
- **Audit ID:** `GATE345-VACUUM-PARAMETER-CENSUS-MINIMAL-INPUT-THEOREM`
- **Layer:** Bridge / Project Meta-Theorem / Vacuum Selection
- **Purpose:** convert the accumulated failed-route ledger into a formal landscape-vs-vacuum census, and count the minimal remaining external inputs after the ASHA finite spectral architecture has derived the Standard Model landscape and its exact boundary ratios.

---

## Core conclusion

Gate 345 formalizes the project-level pattern:

```text
ASHA derives the landscape.
ASHA does not yet derive the unique physical vacuum point.
```

The repeated failed routes are therefore not random failures. They fall into two structural classes:

| Type | Meaning | Examples | Interpretation |
| --- | --- | --- | --- |
| **Type A: Which direction?** | The algebra gives a valid space of possibilities, but not the chosen physical point. | CKM, PMNS, vacuum orientation, flavor projection metric, cosmological vacuum energy. | Fundamental vacuum-selection data. |
| **Type B: How precisely?** | The structure is present, but higher-order continuum precision is missing. | Two-loop RG, pole mass conversion, threshold matching refinements. | Technical precision layer, not landscape failure. |

**Status:** `CONDITIONAL_SUPPORT_FAILURE_LEDGER_CLUSTERED_BY_TYPE`

---

## Absolute landscape results cataloged

Gate 345 catalogs the main native landscape derivations:

| Result | Formula / content | Role | Status |
| --- | --- | --- | --- |
| Gauge and Higgs field content | `U(1)_Y × SU(2)_L × SU(3)_C`, 12 gauge bosons, 1 Higgs doublet | Framework | `CONDITIONAL_SUPPORT_SM_FIELD_CONTENT_DERIVED` |
| Morita split | `κ_C:κ_Q = 1:3` | Framework | `CONDITIONAL_SUPPORT_MORITA_COLOR_SPLIT_DERIVED` |
| Generation topology | `N_gen = 3`, `τ_η = (2,-2,1)` | Framework | `CONDITIONAL_SUPPORT_GENERATION_TOPOLOGY_DERIVED` |
| Weak mixing ratio | `sin²θ_W = 3/8` | Boundary ratio | `CONDITIONAL_SUPPORT_WEAK_MIXING_RATIO_DERIVED` |
| Higgs-to-gauge ratio | `λ_H/g_*² = 1197/4624` | Boundary ratio | `CONDITIONAL_SUPPORT_HIGGS_GAUGE_RATIO_DERIVED` |
| Unified coupling branch | `α_GUT⁻¹ = 8π` under full doubled bosonic trace | Boundary normalization | `CONDITIONAL_SUPPORT_ALPHA_GUT_BRANCH_DERIVED` |
| Hierarchy ratio | `v/M_P = 2^(3/2) exp(-4π²)` | Gravity/electroweak hierarchy | `CONDITIONAL_SUPPORT_HIERARCHY_RATIO_DERIVED` |
| Threshold jump witness | `Δλ ≈ -0.097846792207` | Heavy-sector threshold structure | `CONDITIONAL_SUPPORT_THRESHOLD_JUMP_WITNESS_DERIVED` |

Gate 345 counts four native boundary constraints for the minimal parameter ledger:

```text
sin²θ_W = 3/8
λ_H/g_*² = 1197/4624
α_GUT⁻¹ = 8π
v/M_P = 2^(3/2) exp(-4π²)
```

**Status:** `CONDITIONAL_SUPPORT_FOUR_NATIVE_BOUNDARY_CONSTRAINTS_CATALOGED`

---

## Minimal SM-19 parameter census

Gate 345 uses the conventional minimal Standard Model parameter count:

```text
19 baseline parameters
```

The ASHA-derived boundary constraints reduce the number of remaining minimal vacuum inputs to:

```text
19 - 4 = 15
```

### Remaining minimal vacuum-selection coordinates

| Coordinate block | Count | Kind | Why it remains |
| --- | ---: | --- | --- |
| Charged-fermion Yukawa singular values | 9 | continuous positive | The allowed Yukawa edges are derived, but their numerical singular values are not. |
| CKM texture | 4 | continuous angles/phases | The flavor landscape exists, but the unique quark vacuum orientation is not selected. |
| Strong CP angle | 1 | periodic continuous | Current finite spectral architecture does not fix `θ_QCD`. |
| Absolute unit / electroweak scale | 1 | dimensional scale | ASHA fixes `v/M_P`, not the arbitrary unit used to express GeV. |

Thus:

```text
Minimal SM vacuum manifold dimension = 15 continuous coordinates
```

**Status:** `CONDITIONAL_SUPPORT_MINIMAL_SM19_PARAMETER_CENSUS_FORMALIZED`  
**Status:** `CONDITIONAL_SUPPORT_MINIMAL_VACUUM_INPUTS_COUNTED`

---

## Extended neutrino/cosmology ledger

Gate 345 keeps the extended ledger separate to avoid mixing parameter-count conventions.

Additional model-dependent vacuum coordinates:

| Coordinate block | Count | Kind | Status |
| --- | ---: | --- | --- |
| Neutrino masses | 3 | continuous positive | `FAILED_ROUTE_PMNS_TEXTURE_REMAINS_EXTENDED_VACUUM_COORDINATE` |
| PMNS texture | 6 | angles/phases | `FAILED_ROUTE_PMNS_TEXTURE_REMAINS_EXTENDED_VACUUM_COORDINATE` |
| Cosmological constant | 1 | vacuum-energy coordinate | `FAILED_ROUTE_COSMOLOGICAL_CONSTANT_REMAINS_VACUUM_ENERGY_COORDINATE` |

Therefore:

```text
Extended vacuum dimension = 15 + 10 = 25 continuous coordinates
```

This count is explicitly model-dependent because neutrino and cosmology parameter conventions vary.

**Status:** `CONDITIONAL_SUPPORT_EXTENDED_NEUTRINO_COSMOLOGY_LEDGER_CATALOGED`  
**Status:** `CONDITIONAL_TENSION_EXTENDED_VACUUM_DIMENSION_DEPENDS_ON_NEUTRINO_COSMOLOGY_MODEL`

---

## Discrete / branch-like seals

Gate 345 also records discrete or branch-like choices that are not counted as continuous dimensions in the minimal SM-19 ledger:

```text
- resolvent 2+2 pairing / orientation branch
- electroweak weak-plane sign / orientation
- signed-vs-positive flavor projection metric
- spontaneous scalar carrier orientation
```

These are vacuum-selection branches, not new finite-algebra landscape derivations.

**Status:** `CONDITIONAL_SUPPORT_PHASE_III_VACUUM_COORDINATES_IDENTIFIED`

---

## Minimal input theorem

Gate 345 formalizes the theorem:

```text
The finite Cℓ(1,7) spectral architecture determines the Standard Model landscape
and four exact boundary constraints, but the physical vacuum point requires
15 minimal continuous inputs in the SM-19 convention.

Extended neutrino/cosmology models add 10 further model-dependent vacuum coordinates.
```

This reframes the project:

```text
The algebra built the city.
The remaining inputs are the street address of our vacuum.
```

**Status:** `CONDITIONAL_SUPPORT_MINIMAL_INPUT_THEOREM_FORMALIZED`

---

## Firewalls preserved

Gate 345 intentionally does not:

```text
- insert Yukawa fits
- invent CKM or PMNS textures
- fit the cosmological constant
- force a vacuum direction
- claim two-loop/pole-mass precision
- claim a final theory of everything
```

**Status:** `CONDITIONAL_SUPPORT_VACUUM_CENSUS_FIREWALLS_PRESERVED`

Failed routes preserved:

```text
FAILED_ROUTE_YUKAWA_AMPLITUDES_REMAIN_VACUUM_COORDINATES
FAILED_ROUTE_CKM_TEXTURE_REMAINS_VACUUM_COORDINATE
FAILED_ROUTE_PMNS_TEXTURE_REMAINS_EXTENDED_VACUUM_COORDINATE
FAILED_ROUTE_COSMOLOGICAL_CONSTANT_REMAINS_VACUUM_ENERGY_COORDINATE
FAILED_ROUTE_ABSOLUTE_UNIT_SCALE_STILL_INPUT
FAILED_ROUTE_PHYSICAL_VACUUM_POINT_NOT_DERIVED
FAILED_ROUTE_FINAL_THEORY_OF_EVERYTHING_NOT_CLAIMED
```

---

## Final verdict

Gate 345 succeeds as a project-level meta-theorem.

It proves that the ASHA architecture has not merely accumulated isolated successes and failures. It has found the structural boundary between:

```text
Landscape data: natively derived by the finite Cℓ(1,7) spectral architecture.
Vacuum data: coordinates selecting our universe inside that landscape.
```

The minimal Standard Model input burden is reduced to:

```text
15 continuous vacuum-selection coordinates
```

with an extended neutrino/cosmology ledger of:

```text
25 continuous vacuum-selection coordinates
```

The next valid phase is not to keep forcing landscape algebra to choose a vacuum, but to construct a **dynamical vacuum-selection principle**.
